[project]
name = "agilerl-arena"
version = "0.1.0"
description = "Arena Platform Client and CLI for AgileRL."
authors = [{ name = "Nick Ustaran-Anderegg", email = "dev@agilerl.com" }]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "click>=8.3.0",
    "httpx>=0.27,<1",
    "packaging>=20.0",
    "pydantic>=2.0,<3",
    "python-keycloak>=4.0,<6",
    "pyyaml>=6.0",
    "rich>=13.0,<14",
]

[project.scripts]
arena = "agilerl.arena.cli:main"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["agilerl.arena*"]
namespaces = true
