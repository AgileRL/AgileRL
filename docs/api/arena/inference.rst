Inference
=========

HTTP client for deployed Arena inference endpoints. Handles observation
serialization, batched requests, and response deserialization for real-time
agent interaction.

.. autoclass:: agilerl.arena.inference.Agent
   :members:
